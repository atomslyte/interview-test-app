import React from "react";
import "../styles/hero.scss";

const Hero = () => {
  return (
    <div className="hero">
      <h1 className="hero__title">Watch something incredible.</h1>
    </div>
  );
};

export default Hero;
